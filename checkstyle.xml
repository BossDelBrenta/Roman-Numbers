<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
    <!-- Header con nomi e matricole -->
    <module name="Header">
        <property name="headerFile" value="config/header.txt"/>
        <property name="fileExtensions" value="java"/>
    </module>

    <!-- Lunghezza massima file -->
    <module name="FileLength"/>

    <!-- No tab -->
    <module name="FileTabCharacter"/>

    <!-- Lunghezza massima righe (Checker-level in Checkstyle 8.24+) -->
    <module name="LineLength">
        <property name="max" value="100"/>
    </module>

    <module name="TreeWalker">
        <!-- Complessita' espressioni booleane -->
        <module name="BooleanExpressionComplexity"/>

        <!-- Complessita' ciclomatica -->
        <module name="CyclomaticComplexity"/>

        <!-- Lunghezza massima metodi -->
        <module name="MethodLength"/>

        <!-- No catch vuoti -->
        <module name="EmptyCatchBlock"/>

        <!-- No star import -->
        <module name="AvoidStarImport"/>

        <!-- No import illegali -->
        <module name="IllegalImport"/>

        <!-- Parentesi graffe obbligatorie -->
        <module name="NeedBraces"/>
    </module>
</module>
